1. Валидация формы на js validate
2. ПРоверять либо через 
		if(form.valid())
либо через функцию обработчкика самого validate js

 	submitHandler: function(form) {
			dataFormAjax(form);
		}

Во втором случае не отменяется страндартное действие при нажатии кнопки submit, поэтому в случае любой ошибки будет перенаправление или обновление страницы

ВНИМАТЕЛЬНО ПРОВЕРИТЬ JS СКРИПТ!!! 



Для передачи данных json либо new Formdata